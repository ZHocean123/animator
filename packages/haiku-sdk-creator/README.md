# sdk-creator

SDK into Haiku.

## Setup

After cloning the repo:

```
$ pnpm install
```

Then develop with:

```
$ pnpm run develop
```

## Tests

Run the tests with:

```
$ pnpm run lint && pnpm test
```
